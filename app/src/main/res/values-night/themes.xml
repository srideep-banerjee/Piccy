<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Piccy" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:navigationBarColor">?attr/colorOnPrimary</item>
        <!-- Customize your theme here. -->
        <item name="colorBackgroundTranslucent">@color/white_translucent</item>
    </style>

    <style name="Theme.Piccy.ActionBar" parent="Theme.Piccy">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
    </style>

    <style name="MySearchViewStyle" parent="Widget.Material3.SearchView">
        <item name="searchHintIcon">@null</item>
    </style>

    <style name="Theme.Piccy.Toolbar" parent="Theme.Piccy">
        <item name="searchViewStyle">@style/MySearchViewStyle</item>
    </style>
</resources>